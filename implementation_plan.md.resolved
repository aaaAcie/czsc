# 摩尔缠论架构重构计划 - 核心引擎实现

本计划为在 `czsc` 体系中植入一套完全解耦的“摩尔缠论（Moore CZSC）”量化引擎。代码将统一存放于新建的 `czsc/moore` 包下，复用原有 [RawBar](file:///Users/akuai/code/stock/czsc/czsc/py/objects.py#31-61) 结构，但不使用其特征序列与包含关系处理。

## 摩尔缠论核心概念字典 (The Moore CZSC Definitions)

此量化体系不使用传统“笔”与特征序列，全部依赖绝对极值与 MA5/MA34 收敛和发散物理形态。

### 1. 顶底的确立法则
*   **引擎触发（转折K）：**
    *   **常规触发：** 该K的 `MA5` 停止生长（上涨则 `MA5 <= 前一根MA5`，下跌则反之），且该K线实体**完全反突破**（跨越）MA5。
    *   **跳空触发：** 价格跳空发生，且实体跳空MA5。无视 MA5 生长状态直接确立。
*   **唯一性作废法则：** 如果转折 K 自己就是全线段绝对极值（最高/低），且同时符合四法则，则**强制作废其自身，并顺延其后一根 K 线作为新的转折 K** 进行重新寻找和验真。
*   **实体推升要求：** 新一顶（底）的**实体最高（低）边沿**，必须越过前一顶（底）的**实体最低（高）边沿**。
*   **验真四法则（四位一体触发）：**
    1.  **3K 组合与独立性：** 中间一根价格绝对最大（小）。顶或底至少有一个端点，拥有连续 2 根整根价格区间完全绝对独立在前一中枢以外的 K 线。
    2.  **两侧大铡刀：** 这个向上的顶与向下的底在此期间，必定发生过一次 **MA5 上/下穿 MA34**。并且底与顶须在这交叉点的两端。
    3.  **线段中枢包裹：** 顶底之间必须存在一个完全闭环构造出的**线段中枢**。
    4.  **时间最近原则：** 沿着寻找路线取最近的触发点。

### 2. 线段中枢的双极轨道法与重组规则
放弃三笔重叠主观画法，采纳肉眼可见和非肉眼中枢两种轨制，中枢区具有绝密锁定状态机。

**[规则 A：中枢选取唯一律]**
*   **同向不重叠：** 同方向上的前后两个中枢**不能够有重叠**。以新生中枢线完成线段中枢的当下开始往回看，绝不与前一中枢有任何的 K 线共用（除了中枢连接段的“共用 K/走势”作为旧结束和新开始）。
*   **肉中枢霸权（线段重组）：** 肉眼可见类重叠中枢具有最高优先级。如果**肉眼可见中枢与前一非肉眼可见中枢发生重合，则非肉眼中枢无效**。

**[规则 B：两轨定位法]**
*   **肉眼可见中枢**：
    *   由 MA5 密集重叠快速触发。
    *   **定轨**：一条轨道由“确认K”（即刺穿重叠去的起爆K）确定，另外一条轨道：上升趋势时取此区域内 `MA5` 锯齿波动的**第一个波峰**；下降趋势时取首个 `MA5` 波谷。
*   **非肉眼可见中枢**：
    *   **步骤一：抓取锚点 K0**。游标只找一根：未跟已有中枢发生一丝交集（全价格剥离），且实体绝对站在 MA5 正侧生长的单薄 K 线。
    *   **步骤二：等待确认 K（中枢线 K）**。游标停住，等到出现一根实体完全反向刺破 MA5 的“确认 K”落位。
    *   **步骤三：提取中枢线**。常规下就是“确认 K”刺穿 MA5 后形成的那一段反向实体端（比如收盘价）。双跳空下，则是确认K跳出 MA5 的最末实体端。
    *   **步骤四：三式成立（三选一）**：
        1.  **反正两穿**：当前确认K 往往右看。K+1 的实体反穿中枢线（双跳空默认成立），K+2 再次正穿回原趋势，且 K+2 的绝对高（低）极值超过 K+1。
        2.  **5K 重叠**：确认 K 的完全价格区间与本次波段从极值处首 K 价格区间重合区内，挤进了 5 根以上 K 线穿行（如果跳空只需 4K ）。
        3.  **三笔交替**：首末取向内连着凑齐 3 个反向高点/低点（纯物理运动，不需要满足老缠论里的底分顶分条件），经历“一正一副一回溯”成三势。
    *   **步骤五：非肉眼中枢定轨**：
        *   上升趋势：中枢线就是【下轨】。其往左顺延查找该线段内，**最左边与中枢线有价格交集的连续两根K线的重叠区域的【上轨】**，即为本中枢的【上轨】。
        *   下降趋势：中枢线就是【上轨】。其往左顺延查找该线段内，**最左边与中枢线有价格交集的连续两根K线的重叠区域的【下轨】**，即为本中枢的【下轨】。

---

## 目标与改动设计

### 1. [NEW] `czsc/moore/objects.py` - 数据承载层
定义摩尔缠论专用的各类状态实体类，全面摒弃原有的 `FX/BI/ZS` 体系。
*   `TurningK`: 极值转折K线。包含价格、时间戳、索引、及四法则过滤状态。
*   `MooreCenter`: 双轨中枢。
    *   划分属性 `type` 为 `VISIBLE` (肉眼可见) / `INVISIBLE` (非肉眼可见)。
    *   属性包含：`anchor_k0`, `confirm_k`, `center_line` (中枢线), `upper_rail` (上轨), `lower_rail` (下轨)。
*   `MooreSegment`: 本质线段。由两次有效转折构成，内部包裹中枢。

### 2. [NEW] `czsc/moore/analyze.py` - 状态机运算引擎
核心业务逻辑类 `MooreCZSC`，提供与原版 [CZSC](file:///Users/akuai/code/stock/czsc/czsc/py/analyze.py#181-430) 类似的流式 [update(bar)](file:///Users/akuai/code/stock/czsc/czsc/py/analyze.py#255-303) 方法，其包含三大步骤：
1.  **MA指标流式计算：** 实时处理传入的 [RawBar](file:///Users/akuai/code/stock/czsc/czsc/py/objects.py#31-61) 生成无延迟的 `MA5` 和 `MA34`。
2.  **顶底寻极游标：** 通过引擎规则触发备选点、维护极大极小列表，四法则随时准备抛停。
3.  **双轨中枢状态机：**
    *   维护内部锁定状态。
    *   高纯度 `K0` 的搜索及被动破防“确认 K”。
    *   起手三式流检算。

## Verification Plan

本阶段主要是基建开发，无UI测试，通过自动化测试与数据比对进行验证。

### Automated Tests
*   在 `tests/test_moore_objects.py`（或测试目录下新建对应文件）内构建合成的标准 Bar 序列。
*   将合成K线输入 `MooreCZSC` 对象，断言所生成的 `TurningK` 的时间戳是否与预期一致，断言反正两穿及非肉眼中枢上下轨的位置取数。

### Manual Verification
*   利用已有的 `czsc.utils.echarts_plot` 对接我们的 `MooreCZSC` 输出对象。
*   将识别出的摩尔线段、摩尔双轨中枢可视化叠加在 K 线图和 MA5/MA34 主图上，由用户主观验收转折与中枢的划分是否贴合摩尔缠论心法。
