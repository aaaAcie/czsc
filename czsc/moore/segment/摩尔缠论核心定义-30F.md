# 摩尔缠论（Moore CZSC）核心引擎定义与逻辑

> 摩尔缠论是一套完全独立于 CZSC 传统笔/线/段体系的量化分析引擎，
> 全部逻辑基于**绝对价格极值** + **MA5/MA34 物理形态**驱动，
> 不使用包含关系处理、特征序列、传统分型等概念。

---

## 一、数据层对象（`czsc/moore/objects.py`）

### `TurningK` — 转折极值 K 线

| 字段 | 类型 | 含义 |
|------|------|------|
| `raw_bar` | `RawBar` | 对极值 K 线的引用 |
| `mark` | `Mark.G / Mark.D` | 顶（G）或底（D） |
| `price` | `float` | 极值价格（顶用 high，底用 low） |
| `k_index` | `int` | 在原始 K 线序列中的绝对索引 |
| `turning_k` | `RawBar` | 转折确认 K（默认等于触发信号K；特殊法则下后移一根） |
| `turning_k_index` | `int` | 转折确认 K 的绝对索引 |
| `trigger_k / trigger_k_index` | 兼容字段 | 与 `turning_k / turning_k_index` 同义映射 |
| `is_valid` | `bool` | 是否通过法则 1、2，顶底正式成立 |
| `is_perfect` | `bool` | **内政**：顶底之间是否存在线段中枢（法则 3，微观几何成立） |
| `is_locked` | `bool` | **外交**：是否已被趋势正式锁定为不可更改的历史锚点（宏观锁定，不可被穿透吞噬） |
| `cache['leg_ma5_extreme']` | `float` | 已确立端点对应线段区间的 MA5 极值快照（用于复盘追溯） |

> **`is_perfect` 与 `is_locked` 的哲学区别**
> - `is_perfect = True`：这个顶底内部走势"结构完整"，有中枢支撑的反弹/回调
> - `is_locked = True`：这个顶底已被下一段反向走势正式"认证"为历史端点，高维定性完成，任何穿透回溯都不能触碰它

---

### `MooreCenter` — 双轨线段中枢

| 字段 | 含义 |
|------|------|
| `type_name` | `"VISIBLE"`（肉眼可见）或 `"INVISIBLE"`（非肉眼可见） |
| `direction` | 中枢所在线段的延伸方向 |
| `anchor_k0` | 状态机锁定的高纯度锚点 K0 |
| `confirm_k` | 实体反穿 MA5 的确认 K |
| `center_line` | 中枢线价格（上升线段=下轨，下跌线段=上轨） |
| `upper_rail / lower_rail` | 上下轨价格（波峰/波谷处可动态升级） |
| `start_dt / end_dt` | 中枢时间范围（`start_dt` 按起手三式方法级左边界确定：5K=转折K后重叠起点，2C≥确认K，三笔=转折前顶/底K） |

---

### `MooreSegment` — 本质线段

由两个连续异向 `TurningK` 构成，内部挂载 `MooreCenter` 列表。
- `direction`：`Up`（底→顶）或 `Down`（顶→底）
- `is_perfect`：由终点 `TurningK.is_perfect` 决定（实线 or 虚线）
- `power`：绝对价格力度 `abs(end_k.price - start_k.price)`

---

## 二、顶底确立引擎（`_update_turning_k_engine`）

### 2.1 触发机关（转折K）

每根 K 线都会先通过“触发门”。触发门的本质是两件事同时发生：

1. 均线节奏发生变化（或出现实体跳空这种强力例外）；  
2. 这根 K 线的实体确实穿到了 MA5 的另一侧。

找顶时（准备向下）：
- 要么 MA5 不再上升，要么出现“实体向下跳空 MA5”的强信号；
- 且当前实体下沿已经落到 MA5 下方。

找底时（准备向上）：
- 要么 MA5 不再下降，要么出现“实体向上跳空 MA5”的强信号；
- 且当前实体上沿已经站到 MA5 上方。

同一根 K 的执行顺序是固定的：
- 先看“同向刷新”能不能成立（延续原方向并刷新端点）；
- 同向本根一旦成立，就不再尝试“反向转折”；
- 只有同向没落地，才会继续尝试反向。

> ⚠️ 转折K 只是"信号灯"，它告诉引擎"此处附近应该产生一个顶/底"，转折K 本身**不是**极值点。

术语对照（便于读代码）：
- “找顶/找底”对应 `Mark.G / Mark.D`。
- “触发门”实现函数是 `_is_turning_triggered`。

---

### 2.2 极值寻址（转折K驱动）

触发后，不会直接把“触发K”当成顶底，而是要先在合法区间里找真正极值。
这个区间的左边界非常关键，它决定了引擎能回看多远。

先讲寻址模式：

- `use_left_3k_locator = True`：从转折K向左扫描，取命中的第一个 3K 形态顶底；
- `use_left_3k_locator = False`：按“先绝对、后局部”的两段式流程：

  1. 先在区间 `[search_start, trigger_index)` 取**绝对极值**（不含触发K）。  
  2. 再看当前段 `[search_start, trigger_index]` 的绝对极值（含触发K）。  
  3. 若二者相等，直接采用步骤1的绝对极值；若不等，则从 `trigger_index` 向左逐个做 3K 扫描，命中的**第一个**作为候选顶底。  
  4. 若左扫未命中，再回退到步骤1的绝对极值兜底。

再讲左边界（最容易出错的地方）：

同向刷新时：
- 左边界从“上一个异向端点之后”开始。
- 直觉上就是：你在延伸当前这一笔，不允许回头跨到更早那一笔去“借极值”。

异向转折时：
- 约束更严格：新分型的“三根K”不能和前一个转折K产生时序重叠；
- 并且新分型也不能吃到上一端点附近的 3K 结构。
- 落到实现上，不仅限制分型中间K，也额外校验“分型第一根K”必须在合法左边界及其右侧。

工程实现补充（与代码一致）：
- Refresh 物理预筛时，会先用 `[last_same_idx + 1, trigger_index]` 这段做一次极值定位。
- 真正确立时若已预定位成功，会直接复用该 `ext_idx`；否则按上面的 `search_start` 规则重算。
- 异向场景有二次保险：不仅检查分型中间K位置，还显式检查分型第一根K不越界。

**⚠️ 价格比较使用真实极值**：同向刷新的价格过滤，必须用搜索到的真实极值价格（`new_price`）与已确认的旧顶底比较，而**不是**用触发K自身的 `bar.high / bar.low`。触发K只是信号，不是极值。

术语对照（便于读代码）：
- “左边界”在代码里是 `search_start`。
- “分型中间K”是 `ext_idx`；“分型第一根K”可理解为 `ext_idx - 1`。

---

### 2.3 唯一性作废法则（转折K自身是极值时）

**触发条件**：搜索到的极值K 的索引 == 原始信号K 的索引（`extreme_k_index == actual_trigger_index`）

**处理**：
- 转折K（`turning_k`）自动顺延至其后的**第一根 K 线**（即确认K）
- 极值K 保持不变，仍以搜索到的最高/低点为准
- 目的：转折K 代表"一根线段的新生"，它本身不能是 3K 中间的极值K

```
正常：极值K ←→ 转折K（不同根）
特殊：极值K = = 触发K → 转折K 右移一位 → 极值K ←→ 转折K（相邻）
```

---

### 2.4 候选点管理（`candidate_tk`）

**极值K 与候选K 的关系**：

> - **极值K**（`extreme_bar`）：2.2 搜索完成后得到的那根原始 K 线，强调物理定位——"这根 K 是价格高点/低点所在"。  
> - **候选K**（`new_tk`）：将 `extreme_bar` 包装为 `TurningK(raw_bar=extreme_bar, turning_k=触发K, ...)` 后进入 `candidate_tk` 托管，强调逻辑状态——"这根 K 正在等待法则验真"。  
> - 两者指向同一根 K 线，只是不同阶段的称呼：极值K 是搜索的输出结果，候选K 是状态机的管理单元。

候选处理分两条链路：同向刷新（延伸）与异向转折（反向新生）。

#### 2.4.1 同向刷新链路（Refresh）

前提：本根先通过同向触发门（2.1）。

步骤 A（先看 MA5 是否“更优”）：
- 对比“旧区间”和“新区间”的 MA5 极值。
- 如果 MA5 已经刷新，直接进入同向寻址；这一步成立后，不再检查步骤 B。

步骤 B（仅当 A 不成立）：
- 只在“旧触发K之后到新触发K之前”这段右侧窗口里找最优价格；
- 价格必须刷新旧区间极值；
- 并且实体必须做出方向性“严格穿透”（严格大于/小于，等于不算）。

边界点：
- 即使影线创了新高/新低，只要实体没有严格穿透旧端点实体边界，刷新仍然失败。

同向刷新结果：
- A 或 B 任一成立，才会构造刷新尝试。
- 若同向命中的是“同一个极值K”，不新建端点，只更新时间触发锚。
- 若同向尝试失败，只丢弃新候选，不回退旧端点。

#### 2.4.2 异向转折链路（Reversal）

前提：同向未落地且本根通过反向触发门（2.1）。

执行顺序：
- 先按 2.2 的异向边界寻址（含第一根K边界与二次保险）。
- 再过“异向运行基线门槛”（2.5）。
- 通过后进入候选与四法则验真。

**无旧候选时（`candidate_tk is None`）**：直接设置新候选，无需比较条件。

每根 K 线进入时，**优先执行一次候选校验**：若通过则立即确立；若不通过则立即废弃，不跨 K 线悬置等待。

术语对照（便于读代码）：
- “步骤A/步骤B”对应 `_prepare_refresh_attempt` 里的 `cond_a / cond_b`。
- “同一个极值K仅更新时间锚”对应 `ext_idx == s.turning_ks[-1].k_index` 的分支。

---

### 2.5 异向候选的 MA5 + 价格 运行基线门槛（新增）

为避免异向场景在早期缺乏稳定基值时误判，引擎对异向候选引入运行态双基线：

- 基线维护在 `SegmentState`（运行态）：
  - `reversal_ma5_gate_mark`
  - `reversal_ma5_gate_start_k_index`
  - `reversal_ma5_gate_extreme`
  - `reversal_price_gate_mark`
  - `reversal_price_gate_start_k_index`
  - `reversal_price_gate_extreme`
- 每次异向候选出现时，计算区间 `[last_turning_k.k_index, current_trigger_index]` 的 MA5 与价格极值：
  - 找顶：`max(ma5)`
  - 找底：`min(ma5)`
  - 找顶价格：`max(high)`
  - 找底价格：`min(low)`
- 与运行基线比较时，满足其一即可放行（OR）：
  - MA5：找顶 `current_max_ma5 > baseline`；找底 `current_min_ma5 < baseline`
  - 价格：找顶 `current_max_price > baseline`；找底 `current_min_price < baseline`
- 运行基线上下文切换（方向变化或段起点变化）时，对应 baseline 视为重置（首个候选默认通过该维度）。
- 无论候选最终是否确立，MA5 与价格两条 baseline 都会按当前候选推进。

> 关键口径：即使该异向候选最终未能确立，运行基线也会推进更新；
> 也即“被丢弃候选同样参与 MA5 与价格基线演化”。

另外，已确立 `TurningK` 会在 `cache` 落盘该段 MA5 极值（`leg_ma5_extreme`），用于复盘追溯。

术语对照（便于读代码）：
- “运行基线”是状态里的 `reversal_*_gate_*` 三元组（方向、起点、当前极值）。
- “放行 OR”是：MA5 刷新或价格刷新任一成立即可。

### 2.6 特殊法则（转折K后移一根）

**触发条件**：当次触发流程中，若“转折K本身就是本段极值K”（`extreme_k_index == turning_k_index`），且该候选点通过四法则验真。

**处理**：
- 当前转折K作废（仅作触发信号，不作为最终转折K）。
- 极值K保持不变。
- 将转折K后移一根：下一根 K 线作为新生转折K，再次调用 `_process_confirmed_trigger` 完成确立。

**核心约束**：该法则只改变 `turning_k` 的定位，不改变顶底极值搜索结果。

---

## 三、四法则验真（`_validate_four_rules`）

每次 `candidate_tk` 准备确立时，必须通过以下全部法则：

### 法则 1：3K 极值要求
`tk.raw_bar` 必须是局部极值，即在原始 K 线序列中满足：
- 顶：`curr.high >= prev.high AND curr.high >= next.high`（宽容的平顶法则：允许巨型平顶阵列的存在，取最近的一根极值）
- 底：`curr.low <= prev.low AND curr.low <= next.low`（宽容的平底法则：允许巨型平底阵列的存在，取最近的一根极值）

不满足 → 返回 `(False, False)`，彻底拒绝

### 法则 2：两侧大铡刀（MA5/MA34 金死叉）
在参考点 `ref_tk` 到当前 `tk` 之间，**至少发生一次 MA5 穿越 MA34**（金叉或死叉）。

当前实现放松了扫描边界：  
从“起点3K中间K到终点3K中间K”，扩展为  
“起点3K第一根（`start_idx-1`）到终点3K最后一根（`end_idx+1`）”。

不满足 → 返回 `(False, False)`，彻底拒绝

> **`ref_tk` 确定方式**：
> - 若上一个确立点与当前 `tk` **同向**（同向刷新场景）→ `ref_tk = turning_ks[-2]`（往前多跳一个）
> - 否则 → `ref_tk = turning_ks[-1]`

### 法则 3：结构完整性（级别保障，决定虚实）
引擎会检查 `ref_tk` 到当前 `tk` 之间是否存在已通过“黑K质检”的中枢：
- **实线段** (`is_perfect = True`)：代表该线段内部结成了具备物理支撑的中枢，结构稳固。**或者满足“独立脱离的两K法则”：只要线段的顶峰或谷底，有两根连续的极值 K 线能够完全独立（不沾边地高过或矮过）于该线段内的所有中枢，它就直接获得实线金牌，绝不被判定为内部扰动。**
- **虚线段** (`is_perfect = False`)：虽然满足了极值和金死叉法则，但因内部既缺乏中枢，又未能完成独立脱离，被视为**级别不足**或**结构单薄**。在可视化中以虚线表示。

> ⚠️ **级别保障心法**：
> “中枢成立即线段成立”。若一个波段无法在内部结成包含黑K的中枢，其本质只是主趋势中的一段微观扰动。

### 法则门控开关（valid / perfect）

- `ma34_cross_as_valid_gate = True`（默认）：  
  `is_valid = rule1 and rule2`，`is_perfect = rule3`
- `ma34_cross_as_valid_gate = False`：  
  `is_valid = rule1`，`is_perfect = rule3 and rule2`  
  （即 MA5/MA34 交叉只影响虚实，不阻断顶底成立）

---

## 四、宏观物理审计层（`_macro_audit_and_replay` & `_check_leap_physics`）

> 核心哲学：**站在未来审判过去，用物理证据抹平中间的噪音陷阱**

### 4.1 双世界与提交快照

现实现采用“微观世界 + 宏观世界”双轨：

- **微观世界**：持续流式生长，允许末端同向刷新（最后一个端点可被替换）。
- **宏观世界**：只做吞噬审计与塌陷，不接受微观末端回写替换。

二者同步方式是 **Committed Snapshot（已提交快照）**：
- 同步源不是全部微观顶底，而是 `micro_turning_ks[:-1]`（最后一个可刷新端点不参与同步）。
- 宏观同步为 **append-only**（只追加不回退），避免出现“宏观端点被微观同向刷新带跑”的问题。
- 宏观吞噬后产生的排除集（`macro_excluded_micro_ids`）会在后续同步中持续生效。
- 同步引擎会为微观点写入/维护 `micro_id`，并在宏观点中保存 `source_micro_id` 映射，保证双世界可追溯。
- 吞噬映射（`macro_swallow_map`）记录“宏观一段吞了哪些微观 ID”，供重建宏观线段与图表提示使用。

### 4.2 审计触发条件与疑点优先级

当某个端点 `Vx` 因结构不完美被打上 `maybe_is_fake=True` 后，不会立刻审判。

审计必须同时满足：
1. 宏观端点数量达到启动门槛（`len(macro_turning_ks) > 4`）；  
2. `Vx` 右侧已积累至少 `audit_link_rounds` 个**已提交宏观点**。

疑点处理策略：
- **老疑点优先**：每轮只取最早的 `maybe_is_fake`；
- **每轮只处理一个疑点**：成功吞噬即结束本轮，等待下一次快照推进后再开新审计。
- `maybe_is_fake` 标记挂在虚线段终点，审计坐标直接使用该终点索引（`idx = fake_idx`）。

### 4.3 深度多层审计与右侧法官池

对被选中的单个疑点 `idx`，先执行一个预备轮（Pre-Round）：

- 固定尝试：`start = idx-2`，`end = idx+1`
- 仅使用法则一（生长法则）做物理边际审判
- 若预备轮失败，完整回退到正式轮次，不改变原主流程

正式轮次中，左侧锚点按轮次回溯：
1. 第一轮：`start = idx-1`
2. 第二轮：`start = idx-2`
3. ...直到 `audit_link_rounds`

每轮固定一个左锚后，右侧法官池为 `[idx, n]`，并按从近到远尝试；只接受与左锚异向的 `tk_end`。  
并且必须满足“存在实际中间节点可吞噬”（`end_idx > start_idx + 1`），相邻端点不允许判定为成功吞噬。  
这保证“同一锚点内先小吞噬，再大吞噬”；若小范围可解，不拖到下一轮大范围再解。

### 4.4 物理审判（`_check_leap_physics`）

找到合格的候选法官后，必须经过严苛的双轨物理交叉验证：

首先构建两段区间并比较“趋势极值”：
- `old_scope = [seg_start, old_trigger]`（旧法官触发前的趋势区间）
- `new_scope = [seg_start, new_trigger]`（新法官触发前的趋势区间）
- `ma5_is_better` 不再用单点 MA5，而是用区间 MA5 极值是否刷新（上攻看 `max`，下行看 `min`）。
- 价格生长同样使用区间价格极值是否刷新，再叠加实体越界约束，避免单根顶底K的机械比较失真。
- 与微观引擎共用 `segment/scope_utils.py` 的统一刷新判定口径。

**法则分类：**
| 判决法则 | 适用场景 | 通过条件 | 物理意义 |
|---------|---------|---------|----------|
| **法则一（生长法则：物理边际审判）** | `ma5_is_better == False`（背离/弱势） | `new_scope` 的价格极值刷新 `old_scope`，并且**K线实体层面前后超越**障碍标杆。 | 虽然均线没跟上，但趋势价格已在区间维度完成暴力刺穿，承认其突破！ |
| **法则二（延伸法则：重心与引力双重审判）** | `ma5_is_better == True`（顺势） | 满足上述物理生长的同时，路径内所有 MA5 **不能刺穿**“起点顶底到其转折K区间”的 MA5 趋势极值（上攻看区间最低，下行看区间最高）。 | 允许顶底之后到转折K之间的 MA5 延续生长/衰减，重心锚定更贴近真实趋势发力区。 |

### 4.5 时空坍塌与打入幽灵（Collapse）

一旦找到满足物理法则的法官对某一层起始跳板成功判决：
1. **打入深渊**：将中间被跨越的障碍物们打包扔进 `ghost_forks`（幽灵分叉堆）中。这就是最高维度的清理操作——“历史虚线没有被消除前，不对后续做出误判”。
2. **时空重连**：起跳点 `tk_start` 与 落脚点 `tk_end` 被电焊般连接在一起，成为新世代不可否认的历史粗主干。这终止并收敛了该层级内的所有纠葛。
3. **洗清嫌疑与上钢印**：去掉了终点的虚假标签；对经历考验的后端主干统一赐予 `is_locked = True` 的锁死令，成为后面几轮扫描的不可推倒的石碑。

如果整整拉出了三轮深度扫描都没通过物理审判怎么办？
- 沉默，等待行情推移。当下一次产生新的拐点 $N+2$, $N+3$ 时，这批点将再次充实右侧法官列表，带着比上一代法官更足的能量，向过去的弱点再次下发神谕。

### 4.6 幽灵分叉记录呈现

在最终可视化的图表中，上述被“打入深渊”的中间结构并没有被数据丢弃。它们会保留原本的走势价格，以**灰色点划线从真实的实线锚点处出发，画出一条断头的枝丫**。
这直观地展示了市场在这段时间内制造的诱假动静，是极其珍贵的复盘陷阱化石。

### 4.7 图表展示约定（双世界）

- 微观层：继续展示原始顶底与原始线段（来源世界）。
- 宏观层：展示全部宏观线段（结果世界）。
- 被宏观吞噬覆盖到的微观线段，用灰色弱化显示。
- 宏观吞噬线段保留内部微观 ID 映射，便于复盘“吞噬了哪些微观段”。

---

## 五、趋势状态维护（`_update_trend_state` + `_flip_trend`）

### 状态变量

| 变量 | 含义 |
|------|------|
| `trend_state` | 当前趋势方向（`Direction.Up / Down`），初始为 `None` |
| `trend_high` | 趋势内全局最高价 |
| `trend_low` | 趋势内全局最低价 |
| `trend_extreme_k` | 代表全局极值的 `TurningK` |

### 初始化
第一根有效线段（`turning_ks >= 2`）确立后，扫描全部现有顶底初始化趋势方向与极值。

### 极值更新
每次新 `TurningK` 确立后，若价格刷新了对应方向的全局极值，则更新 `trend_high / trend_low` 与 `trend_extreme_k`。

### 趋势翻转双重锁（满足其一即翻转）

**上涨趋势 → 下跌翻转：**
1. **V 型反转**：新底价格 < `trend_low`（直接打穿全局最低）
2. **结构翻转**：新底 `is_perfect == True` 且突破上一线段起点价格

**下跌趋势 → 上涨翻转：**（对称逻辑）

翻转后：`_flip_trend` 重置极值，以翻转触发点为新趋势的起始锚。

---

## 六、双轨中枢状态机（`CenterEngine`）— 观测病房（Pending Center）

### 运行时机
每根 K 线进入后，**先于**顶底引擎执行。

### 核心概念（先讲人话）

中枢引擎在每根 K 线上做的事可以概括为一句话：
先找一个“干净种子”（K0），再等“确认K”立起中枢线，之后在价格轨道里观察它是继续生长、被内部夺权，还是在边界处正式闭库。

这里有三个核心边界概念：

1. **线段物理锚点（山顶/山谷）**  
它是这条线段在价格上的起点。中枢向左回看时不能越过它。

2. **转折确认点（转折K）**  
它是新线段在时间上的法定起点。K0 搜索不能早于它。

3. **叹息之墙（上一个同向中枢破窗点）**  
它是跨中枢的时间防线，新中枢绝不允许向左穿透它。

如果你后续要对照代码，可以把这三者理解为：
- “线段从哪里开始算”（物理锚点）
- “从哪根 K 开始允许找中枢”（转折确认点）
- “上一个同向中枢把左边界推到了哪里”（叹息之墙）

### 更新入口的三道防线

每根 K 线进入 CenterEngine 时，先过三道防线：

1. **法定起点防线**：当前 K 若早于转折确认点，直接忽略。  
2. **锚点跨越防线**：若正在孵化的中枢锚点被更右侧的高质量锚点跨越，立即回滚重来。  
3. **同向老中枢重叠防线**：当前 K 若回到同向历史中枢轨道里，K0 资格会被限制（防“借尸还魂”）。

### 三段式状态机（与代码一致）

| State | 你可以理解成 | 核心动作 |
|-------|-------------|---------|
| **0** | 找种子 | 找 K0（实体站在 MA5 正侧；但“转折确认K”本身有法定豁免，可直接当 K0） |
| **1** | 等确认 | 等第一根实体反穿 MA5 的确认K；期间若回到老中枢防区则种子作废回滚 |
| **2** | 观测病房 | 已有轨道，逐 K 观察：在轨延伸 / 破窗预闭库 / 未成型时允许内部抢占 |

### State 0 与 State 1 的边界细节

- K0 不只是“纯洁”问题，还受“是否与同向老中枢重叠”约束。  
- 转折确认K具有豁免权：即便它不满足纯洁实体，也可成为 K0。  
- 一旦进入 State 1，若后续纯洁 K 回到老中枢防区，当前种子立即作废（回滚）。
- 确认K定义非常硬：实体反穿 MA5 即确认，并立刻进入 State 2 建轨。

### 四类“左边界”要分清（避免混淆）

CenterEngine 里最常见的误解，是把所有规则都当成“同一个起点”。实际上是四套起点：

1. **K0 搜索起点**：不能早于“转折确认点”。  
2. **建轨时向左找 2K 重叠的起点**：不能早于“线段物理锚点”，也不能穿透“叹息之墙”。  
3. **5K 重叠法的扫描起点**：从“转折确认点及其右侧”开始，再叠加“叹息之墙”约束。  
4. **三笔法的扫描起点**：从“转折确认点之前的线段顶/底锚点”开始，再叠加“叹息之墙”约束。

这也是“同一中枢里，不同判定法的时间左边界可以不同”的根本原因。

### State 2：建轨、延伸、预闭库、抢占

#### A. 入场建轨（确认K出现时）

确认K出现后，会立刻确定双轨：

- 一条轨道（中枢线）由确认K实体决定；  
- 双跳空场景与常规场景采用不同取值端（代码里有对称处理）；  
- 另一条轨道通过“向左找最早连续 2K 重叠”确定；  
- 向左搜索严格受两道边界约束：  
  - 不能越过当前线段物理锚点；  
  - 不能越过叹息之墙。

若没找到合法 2K 重叠，则退化为“上下轨同价”（单线起步）。

#### B. 在轨延伸与破窗预闭库（这是最容易误读的点）

当前实现不是“连续三根脱轨才闭库”。真实逻辑是：

- 只要价格离开轨道，先进入“预备闭库”并冻结右边界（破窗前最后一根在轨 K）；  
- 若价格重新回轨，则取消预备闭库，继续向右延伸；  
- 只有在线段边界到来（极值K出现）时，才把“预备”变“正式固化”。

也就是说：**破窗是条件，线段结束是结算时机**。

#### C. 内部抢占（仅限未成型阶段）

只在“尚未形成名分或黑K未通过”时允许抢占，并且要满足：

- 当前已处于破窗态（不在轨道内）；  
- 有更新的备选 K0；  
- 用新 K0 + 当前破窗K 重算出的中枢线，相对旧中枢线是“顺方向前进”的。

满足后，旧病房作废，原地重建。

一旦中枢已经“名分 + 黑K”双通过，内部抢占关闭。

### 闭库时机与右边界对齐（`seal_on_boundary`）

在线段结束（极值K）时强制结算：

1. 若处于预备闭库：直接使用冻结的右边界固化。  
2. 若仍在轨道内：右边界不能包含极值K本身，需回退到极值K前一根（当且仅当不越过确认K）。  
3. 固化尝试结束后，无论成功与否都回滚状态，准备下一线段。

### 名分与黑K：何时算“真正成型”

中枢成型门槛是两段式：

1. **名分成立**（起手三式任一）：优先顺序是 5K重叠 → 反正两穿 → 三笔。  
2. **黑K通过**：名分成立后再检查黑K。

黑K边界细节：
- 5K名分：黑K窗口从“5K法定起点”到当前右边界；  
- 反正两穿 / 三笔：黑K窗口按“确认K到当前右边界”截取，但质检从确认K之后第一根开始（更严格）；  
- 已确立顶底K不能拿来当黑K。

一旦名分成立，会立即按名分类型同步“中枢时间左边界”（只改时间，不改价格轨道）。

### 起手三式与时间左边界（CenterEngine 口径）

- **5K重叠**：从转折K及其后起算（同时受叹息之墙约束）。  
- **反正两穿**：不早于确认K。  
- **三笔**：从转折K之前的线段顶/底锚点起算（同时受叹息之墙约束）。

这三条只影响时间起点（`start_k_index / start_dt`），不改变轨道价格。

### 核心动力学法则

#### 1. 黑K质检（级别之魂）
中枢生命周期内（从确认K之后，到新生中枢线或顶底之前）必须存在至少一根**非正向跳空 MA5** 的 K 线（即实体或影线触碰到 MA5）。
- **目的**：确保多空双方在均线层面有过实质性的阵地交换，保障线段级别。

#### 2. 定性法则：显性/肉眼 (VISIBLE) vs 隐性/非肉眼 (INVISIBLE)
中枢成立后，系统会追踪从 **“确认K起始的正向一笔”** 形成前的 MA5 运行状态，作为分类特征存储在 `is_visible`：
- **VISIBLE**：正向一笔窗口内，MA5 出现过反向运行或走平（斜率回撤）。
- **INVISIBLE**：MA5 一路顺滑递进，无任何折回。

#### 3. 判定方式（起手三式）
不论中枢最终被判定为显性还是隐性，其之所以能成立，必满足以下三种判定图谱的其中之一（存入 `method` 属性）：

| 式 | 名称 | 扫描时间范围 | 核心逻辑 |
|----|------|--------------|---------|
| 一 | **反正两穿** | 从确认K开始向右扫描（不会早于确认K） | 先出现一次“反穿中枢线”，再在其右侧出现“正穿回中枢线且有进展”；若确认K与K0是双跳空，则可直接成立 |
| 二 | **5K重叠** | 从转折确认点及其右侧扫描，再受叹息之墙约束 | 先找到首破，再找到破坏，形成有效重叠区；重叠区内累计至少 5 根 K（或 4 根 + 跳空补强），且**重叠区必须覆盖当前中枢线** |
| 三 | **三笔纯势** | 从转折前的线段顶/底锚点开始扫描，再受叹息之墙约束 | 先从极值出发走出反向一笔（至少 3 根），确认K必须落在这笔时间内；再走出正向一笔（至少 3 根），形成正-反-正闭环 |

> ⚠️ 三式均不满足 → 关闭病房不予挂载
> ⚠️ 时间左边界仅影响中枢时间起算（`start_k_index/start_dt`）与三式扫描窗口，不改变 `upper_rail/lower_rail` 价格取值逻辑。

### 起手三式心法

> 三式互为犄角，合力覆盖所有市场性格，缺一不可：

**V型暴跌/暴涨**：MA5 来不及折返，5K 也没时间重叠，三笔结构尚未成形——此时**式一（反正两穿）** 瞬间将其捕获，一个 K1+K2 的拉锯即宣告中枢成立。

**横盘死皮赖脸**：价格上下乱窜，分不清笔，三笔扫描器迷失——此时**式二（5K重叠）** 用粗暴的阵地战逻辑判定：只要重叠区内挨够了 5 根 K，中枢成立。

**阶梯式阴跌/缓涨回调**：既不暴力（躲过式一），也不横向重叠（躲过式二），走出斜向下/上的 N 字型——此时**式三（三笔纯势）** 启动扫描器，只要确认K落在 N 字型的中间那笔里，当场确诊。

### 终极空间审判与三大延伸法则

中枢通过“名分 + 黑K”后，进入固化审判。核心问题只有一个：
新中枢能否与最近同向中枢“完全空间分离”。

判定口径（与代码一致）：
- 上行段：新下轨必须严格高于旧上轨；
- 下行段：新上轨必须严格低于旧下轨。

完全分离：独立挂载新中枢。  
未完全分离：进入吞并/延伸机制（如下表）。

| 法则名称 | 触发条件（皆为未完全空间分离） | 处理逻辑 | 核心目的 |
|----------|----------|----------|----------|
| **新皇登基** | **新(肉) vs 老(隐)** | **新中枢为肉眼且老中枢为隐形**时，废黜原有隐形中枢，由新中枢执旗。**注意：肉眼不废肉眼**（若两者皆为肉眼则执行标准轨道合并延伸）。 | 尊重可见结构的最高优先级 |
| **老肉吞新灵** | 老中枢为显形，新中枢为隐形 | 新探测结果作废，新中枢被“招安”，其经历的岁月时间（`end_dt`）由老中枢全额继承。 | 强调已有强结构的稳定性 |
| **灵灵相惜** | 双中枢皆为隐形（非肉眼）相撞 | 保持首个隐形中枢，吸收第二个的时间遗产。 | 防止微观碎波干扰已有结界 |

> ⚠️ **水位线同步（防死循环）**：
> 无论新中枢是挂载还是被吞并，叹息之墙都会推进到“本次中枢最后在轨K的下一根”（破窗K），以确保下一次 K0 搜索从新的时空边界开始。

### 遗留的“异类幽灵中枢”（Ghost Center）
由于物理时间的不可逆性，当遇到宏观主线段发生“同向极值刷新”时（原先的顶/底被判定为伪顶底，使得包含它的线段被暴力拉伸以吞并这段行情）：
- **物理机制不变**：微观中枢一旦在区间内被客观事实建立，将绝对保留（例如：在红色的上升宏观线段中，包裹着这段时间内由于短暂洗盘所建立出的粉色“向下中枢”残骸）。
- **图表展示**：在可视端，所有这期间方向与最终宏观线段方向相悖的中枢，将被统一定义为 `Ghost Center (👻异类-中枢)`。它将被渲染为**幽灵灰色带虚线框**的幽闭病房形态。这种视觉保留，绝佳地记录了这段历史时间里曾发生的筹码堆积和多空绞杀假象，是对交易体感最深刻的还原。


---

## 七、流水线整体顺序（每根 K 线）

```
SegmentAnalyzer.update(bar)
    │
    ├─ 1) 预处理：更新 MA5/MA34
    │      - MA5 满 5 根即写入（不受 MA34 冷启动门限制）
    │      - MA34 未满 34 根：return
    │
    ├─ 2) CenterEngine.update(bar, k_index)
    │
    ├─ 3) MicroStructureEngine.update(bar, k_index, ma5)
    │      - 先校验旧 candidate_tk：通过即确立，不通过即废弃
    │      - 处理特殊法则延后一根的挂起转折K
    │      - 本根触发检测：同向刷新与反向转折并检；同向优先裁决
    │      - 触发后：按开关策略寻址（左侧3K / 绝对极值优先）
    │      - 异向候选增加 MA5 + 价格 运行基线门槛（失败候选同样推进基线）
    │      - 同向命中同极值时仅更新 turning_k，不重建新候选
    │      - 物理过滤 _is_physically_better（法则A/B）+ 四法则验真
    │      - 通过后确立 TurningK，并重建微观线段
    │
    ├─ 4) 同步引擎：_sync_macro_world_from_micro
    │      - 只同步 committed 微观点（micro[:-1]）
    │      - append-only 同步到宏观，不回退不替换
    │      - 维护 micro_id/source_micro_id 映射
    │
    ├─ 5) MacroAuditEngine.audit_and_replay
    │      - 老疑点优先；每轮只审一个疑点
    │      - 正式轮次前先做 Pre-Round（idx-2 -> idx+1，法则一-only）
    │      - 满足审计成熟度后启动；左锚多轮回溯，右侧法官池 [idx, n]
    │      - 命中后执行塌陷：记录幽灵、排除被吞噬 micro_id、重连宏观主干
    │
    └─ 6) 若微观发生变化：重播中枢并更新微观线段；若宏观变化：重建宏观线段
```

---

## 八、可视化层（`sz500_moore_plot.py`）

图表叠加“微观来源层 + 宏观结果层”：

| 层次 | 样式 | 语义 |
|------|------|------|
| **微观线段**（细，半透明） | 上涨粉 / 下跌绿 | 原始生长世界（来源） |
| **被吞噬微观线段**（灰色） | 细线 | 已被宏观吞噬覆盖，仅保留复盘痕迹 |
| **宏观线段**（粗） | 实线/虚线 + 吞噬高亮 | 审计后主干（最终判决世界） |
| **幽灵枝丫**（灰色虚线） | 由锚点出发 | 被塌陷打入幽灵堆的历史分叉 |

---

## 九、可配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `max_segments` | `500` | 最多保留的线段数量 |
| `use_left_3k_locator` | `True` | 寻址模式：`True=左侧3K优先`，`False=区间绝对极值优先` |
| `ma34_cross_as_valid_gate` | `True` | `True` 时 MA5/MA34 交叉是顶底成立门槛；`False` 时只影响虚实 |
| `enable_macro_audit` | `True` | 是否开启宏观吞噬审计 |
| `audit_link_rounds` | `5` | 统一左右连接机会：既作为右侧成熟度点数，也作为左侧锚点最大回溯轮数 |

---

## 附录：摩尔缠论量化引擎的"中医心法"映射

这套量化引擎的底层运作，并非简单的数学概率拟合，而是对市场这一"庞大生命体"进行切脉与望诊。这套代码，即是一套完整的电子诊疗系统。

**1. 气血运行：均线系统（MA5 / MA34）**

MA5（卫气）：运行于表，主司防御与极速反应。转折 K 必须由实体反穿 MA5 来触发，这叫"邪气犯表"，是捕捉市场短期异动的最敏锐探针。

MA34（营气）：运行于里，主司底色与滋养。顶底的确立必须伴随 MA5 与 MA34 的金死叉（大铡刀），这代表表皮的伤风感冒已经深入营血，阴阳格局发生了实质性的逆转。

**2. 穴位与骨相：转折 K 与顶底（TurningK）**

转折 K（生穴）：气机刚开始郁结的痛点，随时可能因为新的力量（唯一法则刷新）而转移。

is_perfect（结节）：局部气血淤堵形成的肿块（包裹了线段中枢）。它有抵抗力，能抗住普通的震荡洗盘，但在摧枯拉朽的单边大势面前，依然会被碾碎。

is_locked（骨相）：被宏观大趋势正式册封的历史锚点。肉可烂，气可散，但骨相不可变。只要被 Locked，任何级别的回溯吞噬都不可逾越，它是走势生灭的绝对界碑。

**3. 脏腑成型：本质中枢（MooreCenter）**

中枢是多空双方绞杀最激烈的"中焦脾胃"，是走势蓄能的器官。K0 锚点是实体纯正、未受污染的 K 线，代表多/空方在结营扎寨前的最后一口"纯正真气"。

**4. 生杀大权：宏观物理审计与天人合一（Macro Physics Audit）**

市场的结构是嵌套的，最高维度的"主趋势"拥有绝对的生杀大权。但这不再是武断的价格吞并，而是给予了**“滞后审计”的理性**。

顺势延申（不治之治）：在主趋势未被证伪前，所有未结成完美中枢的反向折腾都是"假动作"（被打上 `maybe_is_fake`）。引擎先保持观察的定力，允许虚线存活。

跨越坍塌（荡平沉疴）：一旦最新的走势（法官）证明了之前那段孱弱只是一次彻底的诱导，主趋势立刻王者归来。引擎将发射探针跨越陷阱，一旦通过了严苛的 MA5 均线重心法则与绝对价格法则，原本纠缠交错的结构瞬间发生时空坍塌。中间的一地鸡毛被打包扔进灰色的幽灵碑（Ghost Fork）中。这就是最高维度的物理审判，在盘面上留下的，是最干净利落的主线段，以及见证过历史杀戮的陷阱化石。
